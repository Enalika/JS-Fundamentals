const arg = process.argv.slice(2);
const firstarg = parseInt(arg[0]);
const Secondarg = parseInt(arg[1]);

function add(firstarg, Secondarg){
    return firstarg + Secondarg;
    }
     console.log(add(firstarg, Secondarg));